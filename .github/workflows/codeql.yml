name: "CodeQL Analysis for Clipvault"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * 0' # Weekly at 12:00 AM UTC on Sundays

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: csharp
          build-mode: none  # Change to "manual" if the project requires building
        - language: javascript-typescript
          build-mode: none  # Change to "manual" if the project requires building

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # Setup dependencies for C# (NuGet) and TypeScript (npm) if required.
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '9.0.x'
    - name: Restore NuGet packages
      run: dotnet restore

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    - name: Install npm dependencies
      run: npm install

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        # Add custom queries if needed
        queries: +security-extended,security-and-quality

    # Manual build instructions (uncomment and modify if build-mode is set to "manual").
    - if: matrix.build-mode == 'manual'
      shell: bash
      run: |
        echo "Building project for manual analysis..."
        dotnet build --configuration Release
        echo "Build completed."

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"
